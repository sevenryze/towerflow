export const enum TowerflowType {
  webApp = "web-app",
  webLib = "web-lib",
  nodeApp = "node-app",
  nodeLib = "node-lib"
}

export enum BuildType {
  production = "producation",
  dev = "development"
}
